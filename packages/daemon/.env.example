# Dev-only overrides. In real use the daemon ships these as build-time constants
# and stores the user's session at ~/.config/ccshare/ after `ccshare login`.
CCSHARE_SUPABASE_URL=https://<project-ref>.supabase.co
CCSHARE_SUPABASE_ANON_KEY=
