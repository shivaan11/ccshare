# deps / builds
node_modules/
.next/
dist/
*.tsbuildinfo

# env & secrets — .env.example files are the only committed envs
.env
.env.*
!.env.example

# supabase local stack
supabase/.temp/
supabase/.branches/

# misc
.DS_Store
*.log
