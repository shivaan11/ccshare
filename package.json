{
  "name": "ccshare-monorepo",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@11.20.0",
  "scripts": {
    "check": "biome check .",
    "check:fix": "biome check --write .",
    "typecheck": "pnpm -r --parallel typecheck",
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.7",
    "typescript": "^7.0.2"
  }
}
